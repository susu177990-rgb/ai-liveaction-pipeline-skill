# 硬规则

## 原视频优先

- 原视频是动作、节奏、构图、镜头运动和表演逻辑的第一事实来源。
- 单帧只是从视频中提取出来的工作资产，不是新的构图起点。
- 没有明确授权时，不改原动作方向、不改原镜头运动逻辑、不改原人物相对位置。

## 主体一致性

- 角色身份不能串。
- 脸部特征不能在连续帧之间漂移。
- 未声明换装时，服装结构、剪裁和颜色关系不能被模型乱改。
- 未声明换道具时，道具外形、持握方向、接触关系和空间位置必须连续。

## 空间与透视

- 原帧地平线、消失点方向、镜头高度、人物占比和景别必须先被诊断，再允许进入融合。
- 场景透视必须匹配原帧结构，不允许只凭审美硬套背景。
- 地面高度、桌面高度、门框位置和前后景遮挡关系不清时，必须先做空背景或补充机位资产。

## 光影与接触点

- 新环境负责最终光源方向、色温、空间反射和接触阴影。
- 原光影只有在与新环境一致时才允许保留。
- 脚底、扶墙、坐靠、持物、倚桌等接触点必须显式记录。
- 接触点不明时，禁止宣称融合结果可用于正式交付。

## 匹配阈值

- `scene_match_score >= 80` 才允许 `direct_fuse`
- `scene_match_score 60-79` 必须 `empty_plate_first`
- `scene_match_score < 60` 必须 `new_camera_needed`

## 预处理层

以下资产层在正式项目中默认为正式层，而不是“有空再做”的补丁：

- `subject_mask`
- `face_mask`
- `outfit_mask`
- `hand_mask`
- `prop_mask`
- `depth_map`
- `pose_map`
- `edge_map`
- `contact_map`

## 提示词边界

- 提示词只负责指令，不负责替代结构分析。
- 提示词不能代替遮罩、深度、姿态、接触点这些稳定层。
- 当结构冲突明显时，先改机位匹配策略，不靠堆提示词硬救。

## 连续性与交付

- 无切镜或弱切镜项目必须检查关键帧之间的身份、服装、道具、光向、空间连续性。
- 任何没有经过 QC 的融合结果都只能标记为中间产物。
- 没有确认项目目录、命名、manifest 和交付摘要前，不算完成项目。
